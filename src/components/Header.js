import {Col, Row, Container} from "react-bootstrap";
import Navigation from "./Navigation";

function Header() {
    return (
        <div>
            <Navigation />
        </div>
)};

export default Header;